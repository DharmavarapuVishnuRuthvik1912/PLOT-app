window.PLOT_CONFIG = {
  // Real app mode: TMDB through local proxy.
  mode: 'proxy',

  // Required only for `tmdb-direct` mode.
  tmdbReadToken: '',

  // Used in `proxy` mode (example: /api).
  proxyBaseUrl: '/api',

  // Region for watch providers and release context.
  region: 'US'
};
